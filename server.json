{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.WYRE-AI/kaseya-vsa-mcp",
  "title": "Kaseya VSA",
  "description": "MCP server for Kaseya VSA RMM — agents, patches, procedures, alarms, tickets.",
  "repository": {
    "url": "https://github.com/WYRE-AI/kaseya-vsa-mcp",
    "source": "github"
  },
  "version": "0.0.0",
  "websiteUrl": "https://github.com/WYRE-AI/kaseya-vsa-mcp",
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/wyre-ai/kaseya-vsa-mcp:0.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "KASEYA_VSA_TENANT_URL",
          "description": "Full base URL including /api/v1.0 (e.g. https://vsa.example.com/api/v1.0)",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "KASEYA_VSA_USERNAME",
          "description": "Local-auth username (omit if using Kaseya One token)",
          "isRequired": false,
          "format": "string"
        },
        {
          "name": "KASEYA_VSA_PASSWORD",
          "description": "Local-auth password (omit if using Kaseya One token)",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "KASEYA_VSA_K1_TOKEN",
          "description": "Kaseya One SSO token (alternative to username + password)",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MCP_TRANSPORT",
          "description": "Transport mode for the server. Set to 'stdio' for local CLI use; the image defaults to 'http' for gateway hosting.",
          "isRequired": false,
          "default": "stdio",
          "format": "string"
        },
        {
          "name": "AUTH_MODE",
          "description": "Credential source: 'env' reads vars locally, 'gateway' expects header injection from the WYRE MCP Gateway.",
          "isRequired": false,
          "default": "env",
          "format": "string"
        },
        {
          "name": "LOG_LEVEL",
          "description": "Log verbosity: debug, info, warn, error",
          "isRequired": false,
          "default": "info",
          "format": "string"
        }
      ]
    }
  ]
}
